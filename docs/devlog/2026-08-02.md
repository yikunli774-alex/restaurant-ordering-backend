# Devlog — 2026-08-02

> 休息几天后回来。先复盘 + 热身,再开始顾客侧。

## 一、热身:确认状态没坏
- 把之前 6 个未推的 commit **推上 GitHub**(RBAC / 登录 / Redis 会话),CI 触发。
- `./mvnw test` → **10/10 全绿**,几天后什么都没坏。
- **真机登录演示**(重建整套含 Redis,经 Nginx 打到两实例):
  - 登录拿 JWT(载荷只有 `sub` + `sid`,无权限)→ `/me` 从 Redis 会话取到 8 个权限 → 登出(204)→ 同 token 再 `/me` **401(即时吊销)** → 错密码 / 无 token 均 401。
  - 证明了「多实例共享 Redis、状态一致、能秒吊销」。

## 二、顾客侧 ①:桌台 + 会话 + 参与者(数据模型)

### 概念(从零)
- **桌台 `dining_table`**:一张桌子,固定编号(T01),贴二维码。
- **会话 `table_session`**:一拨客人从坐下到结账 = 一个会话。**同桌同时只能有一个「进行中」会话**,否则两拨客人账目会混。顾客侧最关键的规则。
- **参与者 `participant`**:扫码进来的匿名顾客,发 token(只存哈希),同桌多人共享一个会话。

### 关键技巧:用「生成列 + 唯一索引」把铁律焊死在数据库里
会话表加一个**自动计算**的列 `active_table_id`:
```sql
active_table_id ... GENERATED ALWAYS AS (
  CASE WHEN status IN ('OPEN','PENDING_PAYMENT') THEN dining_table_id ELSE NULL END
) STORED,
CONSTRAINT uk_table_session_active UNIQUE (active_table_id)
```
- 会话进行中 → 这列 = 桌号;结束 → = NULL。
- MySQL 唯一索引**允许多个 NULL、但不允许两个相同的非 NULL**。
- 于是:同一桌的「进行中会话」最多一个;历史已结束会话(NULL)随便多少张。
- **好处**:这条规则由**数据库本身**保证——并发下(多个请求同时想在一张桌子开会话)也只有一个能成功,应用层不用加锁。这是后面并发正确性故事的地基。

### 写了什么
- `V3__create_tables_sessions_participants.sql`:三张表 + 埋一个 Demo Store 和两张桌子 T01 / T02。
- `table/TableSessionInvariantIntegrationTest`:证明铁律——插一个 OPEN 会话 OK;同桌再插第二个 OPEN → 被唯一索引拒(DuplicateKeyException);把第一个关掉后又能开新的。

### 验证 ✅
`./mvnw test` → **11 个全绿**。

## 三、顾客侧 ②:签名二维码 + 扫码加入会话

### 概念
- **签名桌码**:QR 内容 = `T01.<HMAC 签名>`。改桌号签名对不上 → 拒。防伪。
- **加入会话**:验签 → 找桌 → **找或建**活跃会话 → 发参与者 token(只存 SHA-256 哈希)。
- **并发**:同桌多人同时扫码,靠唯一索引「找或建」——一个建成功,其余撞唯一约束 → 捕获 → 改为加入那个会话。

### 关键并发坑:隔离级别
`join` 是一个事务。MySQL 默认 **REPEATABLE READ** 用「快照」:抢锁失败方在同一事务里重查会话会**看不到**赢家刚提交的行 → 误报找不到 → 500。
改成 `@Transactional(isolation = READ_COMMITTED)`:每条语句看最新已提交数据,失败方重查即见赢家会话并加入。这是并发编程经典坑,也是面试/简历可讲的点。

### 写了什么
- `table/TableCodeSigner`:HMAC 签 / 验桌码。
- `table/TableSessionRepository`:找桌、找或建活跃会话(捕获重复)、插参与者、读会话。
- `table/TableSessionService`:`join`(验签 → 找桌 → 找或建会话 → 发 token)、`getSession`;READ_COMMITTED 事务。
- `table/TableSessionController`:`POST /table-sessions/join`(公开)、`GET /table-sessions/{id}`(公开)。
- `table/StaffTableController`:`GET /staff/tables`(**需店员登录**,列桌 + 可打印的 QR 内容)——第一个被鉴权保护的业务接口。
- SecurityConfig 放行 join / get-session;新增错误码 `TABLE_CODE_INVALID`。

### 验证 ✅(18 个测试全绿)
`TableJoinIntegrationTest` 7 条:建会话 + 发 token / 二次加入复用同一会话 / 篡改签名 400 / 签名有效但桌不存在 404 / 读会话状态 / `staff/tables` 无 token 401、有 token 返回 QR / **16 线程并发扫码 → 恰好 1 个会话、16 个参与者**。

## 五、顾客侧 ③:参与者鉴权(第二道门)

### 概念
- 店员用 JWT(`Authorization: Bearer`);顾客用参与者 token(`X-Participant-Token`)。
- 过滤器读顾客 token → SHA-256 哈希 → 查参与者/会话 → 盖 `ROLE_PARTICIPANT`。
- 两道门互不通:`/table-sessions/**` 要 `ROLE_PARTICIPANT`,`/staff/**` 要店员角色。
- 「只能看自己这桌」:参与者属于会话 A,访问会话 B → 403。

### 写了什么
- `ParticipantPrincipal` / `ParticipantAuthenticationToken` / `ParticipantAuthenticationFilter`。
- `TableSessionRepository.findParticipant(hash)`。
- SecurityConfig:装参与者过滤器 + 顾客/店员路径各要各自角色。
- `GET /table-sessions/{id}` 加「只能看自己这桌」校验(不符 403 `PARTICIPANT_FORBIDDEN`)。

### 踩坑(两个)
1. 过滤器**不做成 @Component**(否则 `@WebMvcTest` 会自动加载 Filter bean、拉出 DB 依赖)——由 SecurityConfig 手动构造。
2. **匿名认证过滤器**在我之前已给请求盖了「匿名身份」,原来的 `getAuthentication()==null` 判断失效、跳过了认证。改成「为空**或匿名**时才认参与者」。

### 验证 ✅(21 个测试全绿)
`TableJoinIntegrationTest` 含:带 token 读会话 200 / 无 token 401 / 跨会话 403 / **顾客 token 打店员接口 403(两道门隔离,对应设计验收「顾客凭证不能调店员接口」)**。

## 六、菜单 / 库存

### 概念 / 决策
- **扁平菜品模型**:菜品直接带 `category` 字段(字符串),不做 sky-take-out 那种分类表 / 口味表 / 套餐表。决策:够用、聚焦点单与并发,后续可扩展。
- **库存单独一张表**(`available` + `reserved`),为后面「加菜时预留库存、防超卖」做准备。
- **沽清(SOLD_OUT)**:菜品状态字段;顾客菜单返回带 `soldOut` 标记(不含 DELISTED 下架的)。
- **菜单读要参与者 token**(扫码 → 加入 → 看菜单)。决策:符合真实扫码流程 + 复用参与者鉴权;想改公开只需一行。
- **管理接口按细粒度权限**:`@PreAuthorize('menu:manage')` / `'inventory:manage'`——**后厨(KITCHEN)进不了菜单管理**(有 ROLE_KITCHEN 过路径,但缺 menu:manage → 403)。
- **启动多建一个后厨账号** `kitchen/kitchen123`,用于演示 / 测试权限分级。

### 写了什么
- `V4` 迁移:menu_item + inventory + 4 个示例菜(宫保鸡丁 / 麻婆豆腐 / 米饭 / 可乐,各 100 库存)。
- `menu/MenuRepository | MenuService | MenuController(顾客只读) | MenuManagementController(店员管理)`。
- SecurityConfig:`menu-items` 要 ROLE_PARTICIPANT;`management/**` 要店员角色。
- `StaffBootstrap` 扩展成建 manager + kitchen 两个账号。

### 顺手修的通用 bug
`@PreAuthorize` 在控制器方法内抛的 `AccessDeniedException` 被兜底 `Exception` 处理器接住 → 变 500。加了 `AccessDeniedException → 403 PERMISSION_DENIED` 处理器。**这影响后面所有 @PreAuthorize 的地方。**

### 验证 ✅(27 个测试全绿)
`MenuIntegrationTest` 6 条:顾客带 token 看菜单 / 无 token 401 / 店长建菜顾客可见 / **后厨不能管菜单 403** / 参与者不能管菜单 403 / 店长设库存。

## 七、购物车(Redis + Lua 原子操作)

### 决策
- 购物车放 **Redis**,`cart:{sessionId}` 哈希(菜品 id → 数量),**同桌共享**。
- **用 Lua 脚本做原子增减**:一次脚本内完成「读当前量 → 加 delta → ≤0 删行 → 版本 +1 → 续期」。多人同时改同一个菜也不丢更新。决策:比「读-改-写」正确,并发正确性硬点。
- 加菜(delta>0)才校验菜品存在 + AVAILABLE(沽清不能加);减 / 删不校验。
- 绝对幂等这版没做(spec 里的 operationId / expectedVersion 幂等留到下单环节);当前是「累加 delta」语义,契合 +/− 加菜按钮。
- 购物车接口在 `/table-sessions/{id}/cart`,已被参与者鉴权 + 「只能动自己这桌」覆盖。

### 写了什么
- `cart/CartRepository`(Redis + Lua `mutate` / `getCart` / `clear`)。
- `cart/CartService`(加菜校验、拼装带单价 / 小计 / 总价的购物车视图)。
- `cart/CartController`:`PUT /cart/items/{menuItemId}`(增减)、`GET /cart`、`DELETE /cart`(清空)。
- `MenuRepository` 加定价查询(按 id / 批量按 ids)。

### 验证 ✅(34 个测试全绿)
`CartIntegrationTest` 7 条:加菜显示 + 总价 / 累加 / 减到 0 删行 / 清空 / 沽清不能加 409 / 不能改别桌购物车 403 / **20 线程并发加同一菜 → 数量恰好 20(Lua 原子性)**。

## 八、加菜下单(order round + 库存预留 + 幂等)

### 决策
- **每次提交购物车 = 一轮订单(order_round)**,立即 CONFIRMED 进后厨;支付留到结账。round_no 每 session 递增,唯一约束兜底。
- **幂等**:必带 `Idempotency-Key`。`api_idempotency(scope,key)→result_id`。重放同 key 返回同一轮,不重复下单 / 扣库存。
- **库存预留 = 条件更新**:`UPDATE inventory SET available=available-q, reserved=reserved+q WHERE available>=q`。并发下行锁串行化,只有够库存的成功,库存永不为负。+ `inventory_ledger` 审计(唯一 operation_id)。
- **购物车原子取出并清空(Lua getAndClear)**:防同一购物车被两人并发提交两轮;下单失败(如库存不足)时把购物车**还回去**(Redis 恢复 + MySQL 事务回滚)。已知小窗口:getAndClear 后进程崩溃会丢购物车(spec 的 freeze-and-reconcile 是完整解,暂缓)。
- 事务隔离 **READ_COMMITTED**(和 join 一致)。
- **消息队列事件暂缓**(OrderRoundCreated 等,等接 RocketMQ);快照菜名 + 单价已做。

### 写了什么
- `V5` 迁移:order_round / order_item / inventory_ledger / api_idempotency。
- `order/InventoryRepository`(条件预留 + ledger)、`OrderRepository`(幂等 / 轮次 / 订单读写)、`OrderService`(下单编排)、`OrderController`(`POST /orders`、`GET /rounds`)。
- 通用:全局异常加 `MissingRequestHeaderException→400`;`TableSessionRepository.findStatus`。
- 测试基建:统一 `TestData.resetSessions`(按外键顺序清理,以后加表只改一处)——顺带修了「加 order_round 外键后旧测试删 session 撞外键」。

### 验证 ✅(41 个测试全绿)
`OrderIntegrationTest` 7 条:下单建轮次 + 清购物车 + 扣库存 / 第二轮 round_no=2 / **重放同 key 返回同一轮不重复扣** / 空购物车 409 / **库存不足 409 且购物车还原、库存不变** / 缺幂等头 400 / **12 线程抢 5 库存 → 恰好 5 成功、不超卖**。

## 九、结账 + 模拟支付(一客一结账闭环)

### 决策
- **结账 CAS**:`POST /checkout` 把会话 OPEN→PENDING_PAYMENT(条件更新,并发只一个赢);账单 = 所有未取消轮次金额之和;建一条 PENDING 支付。
- **结账中不能加菜**:下单已校验会话 OPEN;PENDING_PAYMENT 时提交轮次 → 409 `SESSION_IN_CHECKOUT`。
- **模拟支付**:`POST /pay` 同步直接成功 —— 会话 PENDING_PAYMENT→CLOSED(CAS,并发只一个赢),支付 PENDING→SUCCEEDED。`payment` 表 + status 是接**真实支付的接口预留**(HMAC 异步回调那套暂缓)。
- **关桌后可重开**:CLOSED → 生成列 `active_table_id` 变 NULL → 同桌再扫码开新会话(唯一索引允许)。
- reserved 库存支付后不回退(视为已售);释放只在取消 / 超时(后厨环节)做。

### 写了什么
- `V6`:payment 表。`checkout/CheckoutRepository | CheckoutService | CheckoutController`(`POST /checkout`、`GET /bill`、`POST /pay`)。
- `TestData` 加 payment 清理。

### 验证 ✅(47 个测试全绿)
`CheckoutIntegrationTest` 6 条:结账算账单 + 挂起 / 支付关桌 + 支付成功 / 无轮次结账 409 / 结账中不能加菜 409 / **10 线程并发结账 → 只出一张账单一条支付** / 付完后同桌能开新会话。

## 十、闭环达成 🎉
**扫码 → 菜单 → 购物车 → 反复加菜 → 结账 → 支付 → 关桌 → 桌台可重用**。「一客一结账 + 加菜」完整跑通。
剩:后厨看板(店员推进 CONFIRMED→PREPARING→READY→COMPLETED)、MQ 可靠事件、并发压测出数字。
